# Scraper Script Agent

Use this lane for implementation notes for scraper adapters.

Current scraper entry points:

- `job-scraper-test/src/scrapers/adapters.js`
- `job-scraper-test/scripts/scrape.js`

Keep source-specific parsing isolated so broken sources do not break the entire portal.
